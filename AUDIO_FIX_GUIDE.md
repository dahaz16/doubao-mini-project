# 音频保存问题修复说明

## 🔧 修复内容

### 1. 优化 `interview_service.py` 中的 `save_original_voice()` 函数

**问题**: 数据库连接在长时间的 TTS 处理后超时,导致 AI 语音保存失败

**修复**:
- ✅ 每次保存都使用**新的数据库连接**,避免连接超时
- ✅ 添加**3次重试机制**,指数退避策略(0.5s, 1s, 2s)
- ✅ 改进错误日志,**记录失败的 URL** 和关联信息,便于后续恢复

**代码变更**: `backend/interview_service.py` 第 51-100 行

### 2. 优化 `database.py` 中的连接池配置

**问题**: 连接池配置过于保守,无法处理 Coolify 环境的并发请求

**修复**:
- ✅ 增加最大连接数: `maxconn=10` → `maxconn=20`
- ✅ 增加最小连接数: `minconn=1` → `minconn=2`
- ✅ 添加连接超时: `connect_timeout=10` 秒
- ✅ 添加 SQL 语句超时: `statement_timeout=30000` 毫秒 (30秒)

**代码变更**: `backend/database.py` 第 24-40 行

## 📊 修复效果预期

### 修复前
- ❌ AI 音频保存率: **50%** (12/24)
- ❌ 错误: `server closed the connection unexpectedly`
- ❌ 无法恢复丢失的音频

### 修复后
- ✅ AI 音频保存率: **接近 100%**
- ✅ 自动重试机制,即使首次失败也能成功
- ✅ 详细的错误日志,记录失败的 URL

## 🚀 部署步骤

### 方案 A: 直接部署到 Coolify (推荐)

1. **提交代码到 Git**
   ```bash
   git add backend/interview_service.py backend/database.py
   git commit -m "fix: 修复 AI 语音保存失败问题 (数据库连接超时)"
   git push
   ```

2. **在 Coolify 中重新部署**
   - 进入 Coolify 控制台
   - 找到项目,点击 "Redeploy"
   - 等待部署完成

3. **验证修复**
   - 进行一次完整的采访测试
   - 检查后端日志,确认没有 `保存语音文件失败` 错误
   - 在管理后台查看音频记录是否完整

### 方案 B: 本地测试后再部署

1. **本地测试**
   ```bash
   # 重启后端服务
   cd /Users/wangyituo/Documents/拓的文稿/项目/回忆录项目/回忆录代码项目/doubao-mini-project
   ./run_backend.sh
   ```

2. **进行采访测试**
   - 在小程序中进行一次完整的采访
   - 观察后端日志,确认音频保存成功

3. **确认无误后部署到 Coolify**
   - 按照方案 A 的步骤操作

## ⚠️ 注意事项

1. **已丢失的音频无法恢复**
   - 虽然音频文件已上传到 COS,但数据库中没有 URL 记录
   - 无法通过程序自动恢复

2. **监控建议**
   - 部署后密切关注后端日志
   - 如果看到 `⚠️ 保存语音文件失败 (尝试 X/3)` 警告,说明重试机制正在工作
   - 如果看到 `❌ 保存语音文件失败 (3 次尝试后)`,说明仍有问题,需要进一步调查

3. **数据库配置检查**
   - 确认 Coolify PostgreSQL 的 `max_connections` 设置足够大 (建议 ≥ 100)
   - 确认没有设置过短的 `idle_in_transaction_session_timeout`

## 📝 后续优化建议

1. **添加音频恢复脚本**
   - 扫描 COS 中的音频文件
   - 根据文件名中的时间戳和 user_id 匹配数据库记录
   - 补充缺失的 URL

2. **添加健康检查**
   - 定期检查音频保存成功率
   - 如果成功率低于 95%,发送告警

3. **考虑异步保存**
   - 将音频保存操作放入后台任务队列 (如 Celery)
   - 避免阻塞 WebSocket 响应

## 🎯 总结

这次修复主要解决了**数据库连接超时**导致的 AI 语音保存失败问题。通过:
1. 使用独立的数据库连接
2. 添加重试机制
3. 优化连接池配置

应该能够将 AI 音频保存成功率从 50% 提升到接近 100%。

**建议立即部署到 Coolify 进行验证!** 🚀
